# 个性化 AI 错题辅导设计

> 状态：设计已确认，待实现
>
> 更新日期：2026-08-12
>
> 依赖基线：[AI RAG 学习平台方案](./AI-RAG-LEARNING-PLAN.md)

## 1. 背景与问题

当前 `AI-M0` 已完成结构化知识库、来源引用、资料不足拒答、FastAPI 模型调用和本地回退。正确答案、考点、选项事实和来源由知识库锁定，模型只改写五个讲解字段。

这种实现保证了准确性，但用户感知到的 AI 价值有限：知识库讲解和 AI 讲解结构相同，模型主要在做语言润色，没有充分使用用户的错误选项、累计错误次数、同类错误和复习结果。

下一阶段不应通过“写得更长”体现 AI，而应从“解释题目”升级为“诊断学习者并验证是否学会”。

## 2. 产品定位

知识库与 AI 的职责明确分开：

| 能力 | 知识库讲解 | 个性化 AI 辅导 |
| --- | --- | --- |
| 核心问题 | 这道题为什么这样选 | 你为什么会错、下次怎样避免 |
| 内容来源 | 校对后的固定知识条目 | 知识证据 + 个人错误历史 |
| 用户差异 | 同一道题基本相同 | 按误选项、错误次数和薄弱点变化 |
| 输出重点 | 规则、答案、选项事实、来源 | 错误诊断、判断路径、复习动作、迁移验证 |
| 可靠性策略 | 精确映射、资料不足拒答 | 事实字段锁定、结构化输出、字段校验 |

目标定位：

```text
基于可追溯知识库和个人错题历史的自适应 AI 辅导系统
```

## 3. 设计目标

第一版个性化辅导必须做到：

1. 明确引用用户本次误选项，而不是只解释正确答案。
2. 根据累计错误次数区分首次误选和重复错误。
3. 输出可以复用到同类题的三步判断路径。
4. 对比本题正确考点和用户误选的易混考点。
5. 给出短期、可执行的复习动作。
6. 生成一道受约束的迁移题，现场验证用户是否真正理解。
7. 清楚展示本次个性化分析使用了哪些学习数据。

## 4. 非目标

本阶段不做：

- 开放式聊天机器人。
- 自动替用户修改正确答案、考点或来源。
- 无证据的心理或能力推断。
- 复杂多 Agent 编排。
- 完整长期课程规划和自动排课。
- 不受约束的 AI 自由出题。

## 5. 用户体验

### 5.1 知识库讲解区

继续保留，作为可靠事实层：

- 正确答案与考点。
- 正确选项为什么成立。
- 每个选项的固定事实分析。
- 知识来源与可信度。

### 5.2 AI 个性化辅导区

不再复刻知识库的五段文本，改为六个有明显差异的模块：

1. `你的错误类型`：一句话指出本次误选反映的判断问题。
2. `为什么你会选它`：结合真实误选项和题干线索解释。
3. `三步判断路径`：给出下次可重复执行的步骤。
4. `易混点对比`：只比较本题涉及的两个知识点。
5. `针对你的复习动作`：根据错误次数给出近期任务。
6. `立即验证`：完成一道 AI 迁移题并即时反馈。

面板顶部展示个性化依据：

```text
本次分析依据
误选：にそって
本题累计错误：3 次
当前薄弱点：近义句型辨析
最近同类错误：2 题
```

### 5.3 迁移题交互

迁移题必须经过以下闭环：

```text
AI 基于已锁定知识生成候选题
  -> 服务端校验结构和答案依据
  -> 用户作答
  -> 系统记录是否迁移成功
  -> 更新复习建议和薄弱点状态
```

答对只表示一次迁移验证通过，不直接将原错题标记为彻底掌握；仍沿用现有错题回收规则。

## 6. 输入上下文

FastAPI 生成个性化辅导时使用三类数据。

### 6.1 事实证据，由知识库锁定

- `questionId`
- 题干和选项
- 正确答案
- `testedPoint`
- 正确与错误选项的知识说明
- 来源和可信度

### 6.2 本次作答数据

- `selectedChoice`
- `wrongCount`
- 本次是否为重复错误
- 错题来源模式

### 6.3 最小化学习历史

- 当前主要错误类型
- 最近同类错误数量
- 最近同类考点标识，最多 3 个
- 上一次回收结果
- 最近一次迁移题结果

只发送生成所需的最小字段，不发送完整学习历史。学习历史是诊断依据，不是可由模型改写的事实。

## 7. 输出数据结构

建议新增独立结构，不继续扩充现有知识库讲解类型：

```ts
type PersonalizedTutorExplanation = {
  diagnosisSummary: string;
  whyYouChoseIt: string;
  reasoningSteps: [string, string, string];
  confusionComparison: {
    correctPoint: string;
    confusedPoint: string;
    decisiveDifference: string;
  };
  reviewPlan: Array<{
    timing: 'now' | 'tomorrow' | 'three_days';
    action: string;
  }>;
  personalizationEvidence: {
    selectedChoice: string;
    wrongCount: number;
    weaknessType: string;
    recentSimilarWrongCount: number;
  };
  transferQuestion: {
    prompt: string;
    choices: string[];
    answer: number;
    testedPoint: string;
    explanation: string;
  };
  generationMode: 'ai_tutor';
};
```

知识库讲解和个性化辅导分别缓存：

```text
knowledgeExplanationCache[questionId]
personalizedTutorCache[questionId + learningContextVersion]
```

当错误次数、薄弱点或迁移结果变化时，`learningContextVersion` 更新，旧的个性化分析不再直接复用。

## 8. 服务接口

建议保留现有接口用于可靠讲解，并新增个性化接口：

```text
POST /explain-wrong-answer       # 现有知识库事实 + 受控讲解
POST /tutor/wrong-answer         # 个性化诊断和迁移题
POST /tutor/transfer-result      # 记录迁移题结果
```

`POST /tutor/wrong-answer` 请求示例：

```json
{
  "questionId": "grammar-011",
  "selectedChoice": 1,
  "wrongCount": 3,
  "weaknessType": "近义句型辨析",
  "recentSimilarWrongCount": 2,
  "recentSimilarPointIds": ["grammar_n2_021", "grammar_n2_034"]
}
```

服务端先根据 `questionId` 读取完整题目证据。客户端不负责提交或决定正确答案。

## 9. 生成与校验边界

### 9.1 模型不可修改

- 原题正确答案。
- 原题考点。
- 原题选项事实。
- 知识来源。
- 历史错误次数和薄弱点统计。

### 9.2 模型可以生成

- 个性化错误诊断。
- 三步判断路径。
- 易混点的学习化表达。
- 近期复习动作。
- 受约束的迁移题候选。

### 9.3 服务端必须校验

- 所有必填字段存在且长度合理。
- `reasoningSteps` 必须正好 3 条。
- 迁移题至少 2 个选项且答案索引合法。
- 迁移题 `testedPoint` 必须等于原题锁定考点。
- 迁移题解释不能声称与知识证据冲突的规则。
- 校验失败时不展示半成品，回退到知识库讲解。

## 10. 个性化策略

### 首次错误

重点解释误选项为什么有迷惑性，并给出一个判断规则。

### 重复错误 2-3 次

明确指出重复混淆组合，减少规则复述，增加三步判断路径和立即迁移验证。

### 重复错误 4 次以上

将问题判断为“规则知道但应用不稳定”或“基础规则未建立”中的一种，并安排当前、明天、三天后的分层复习动作。

诊断只能基于可观察行为，不使用“粗心”“能力差”等不可验证标签。

## 11. 评估标准

### 11.1 事实可靠性

- 锁定字段被模型改写率：`0%`。
- 无知识映射时拒答率：`100%`。
- 结构化输出校验通过率：目标 `>= 98%`。

### 11.2 个性化有效性

- 讲解明确提到真实误选项：目标 `100%`。
- 重复错误时明确使用错误次数：目标 `100%`。
- 三步路径包含本题真实线索：目标 `>= 90%`。
- 用户阅读讲解后的迁移题正确率，高于只看知识库讲解的基线。

### 11.3 演示可感知性

使用同一道题准备两个不同用户上下文：

- 用户 A：首次错误，误选「にそって」。
- 用户 B：第 4 次错误，且最近连续混淆回应类和依据类句型。

演示时两份 AI 输出必须在诊断、判断路径和复习计划上明显不同，而知识库事实层保持一致。

## 12. 实施顺序

### AI-M1：个性化诊断

- 新增个性化请求和响应 Schema。
- 汇总最小化学习历史。
- 输出错误诊断、三步判断路径和易混点对比。
- 页面拆分知识库区与 AI 辅导区。

### AI-M2：迁移验证

- 生成受约束迁移题。
- 增加迁移题结构和事实校验。
- 保存迁移结果并更新个性化缓存版本。

### AI-M3：效果评估

- 建立 30-50 道固定评估集。
- 比较知识库讲解与 AI 辅导后的迁移正确率。
- 记录响应耗时、回退率、结构校验失败率和模型成本。

### 后续阶段

- 加入 BM25、向量检索和 Rerank，扩展自然语言知识检索。
- 将个性化辅导扩展到读解证据定位和听力陷阱分析。
- 评估受控 Web RAG，不接入开放式全网搜索。

## 13. 求职展示表述

不再只描述“接入了大模型”，建议表述为：

> 设计并实现基于结构化 RAG 与个人错题历史的自适应 JLPT N2 辅导系统。知识库锁定答案、考点和来源，模型负责个性化错误诊断、可复用判断路径和迁移练习；通过结构化 Schema、字段白名单、失败回退和迁移题校验控制幻觉，并用迁移正确率评估讲解是否真正有效。
