# JLPT N2 Trainer AI 功能说明与面试指南

> 用途：总结项目中的 AI 功能、技术架构、设计取舍与工程边界，用于 AI 应用开发相关岗位的项目介绍和面试准备。
>
> 核对日期：2026-08-27

## 一、项目定位

这是一个面向 JLPT N2 学习场景的 AI 错题辅导与知识检索系统。它不是简单地把题目直接发送给大模型，而是把题库证据、个人错误历史、混合检索、受约束生成、迁移验证和失败回退组合成完整的学习闭环。

面试时可以这样概括：

> 我在一个 React Native 学习 App 中实现了轻量级 RAG 和个性化 AI Tutor。系统先从可追溯知识库锁定答案、考点和来源，再让大模型生成结构化讲解；同时结合用户误选项、错误次数和同类错误生成个性化诊断及迁移题，并通过字段校验、引用白名单、缓存和本地回退降低幻觉与服务不可用风险。

核心技术栈：

- React Native、Expo、TypeScript
- Python、FastAPI、Pydantic
- AsyncStorage
- BM25、TF-IDF、Cosine Similarity
- 可选 OpenAI-compatible Embedding
- OpenAI、DeepSeek、Claude-compatible LLM 调用
- Playwright、Node.js Test、Python unittest

## 二、AI 系统整体架构

```text
React Native / Expo 前端
        │
        ├─ 错题页：RAG 讲解、个性化辅导、迁移题
        ├─ 文法排序页：结构化 AI 解题路径
        └─ 本地进度：错误次数、薄弱点、缓存、迁移结果
        │
        ▼
Python FastAPI AI 服务
        │
        ├─ 知识库事实构建
        ├─ BM25 + TF-IDF + 可选 Embedding 检索
        ├─ 业务规则重排
        ├─ Prompt 构造与 LLM 调用
        ├─ JSON / Pydantic 输出校验
        └─ 受控 Web RAG
        │
        ▼
可信证据层
        ├─ 本地文法、词汇、读解、听力题库
        ├─ 本地 AI 知识映射
        └─ 审批过的官方网站缓存
```

系统遵循一个关键原则：

```text
知识库决定事实，模型负责表达，程序负责校验。
```

## 三、已经接入前端的 AI 功能

### 3.1 基于知识证据的错题讲解

用户做错文法或词汇题后，可以生成以下内容：

- 本题考点
- 错误类型
- 正确选项为什么成立
- 用户误选项为什么不成立
- 干扰项为什么容易误导
- 下次遇到同类题应该注意什么
- 每个选项的逐项分析
- 知识来源

#### 处理流程

```text
用户答错题目
  → 根据 questionId 查找本地题库
  → 锁定正确答案、考点、选项事实和来源
  → 构造本地结构化讲解
  → 调用 FastAPI 或代理服务
  → LLM 只生成允许变化的五个文本字段
  → 客户端和服务端校验返回结构
  → 展示 AI 讲解或回退到本地知识库
```

模型只允许生成或改写以下字段：

- `mistakePattern`
- `whyCorrect`
- `whyUserWrong`
- `whyDistractorFooled`
- `watchNextTime`

模型不能修改：

- `testedPoint`
- `choiceAnalysis`
- `sources`
- 正确答案相关事实
- 置信度等受控字段

客户端合并 AI 输出时使用字段白名单。即使模型返回了额外的答案、考点或来源字段，这些字段也不会被合并到最终结果中。

#### 面试表达

> 我没有让模型直接决定正确答案，而是先由结构化知识库确定事实，再让模型负责教学表达。模型是 explanation generator，不是 source of truth。Prompt 属于软约束，字段白名单和服务端覆盖才是硬约束。

#### 技术边界

错题讲解链路主要通过 `questionId` 精确映射题库证据，严格来说更接近 grounded generation。它没有在每次错题讲解时执行向量 Top-K 检索。真正的混合检索位于独立的知识搜索接口中。

### 3.2 个性化 AI 错题辅导

普通错题讲解回答“这道题为什么这样选”，个性化辅导回答“你为什么会错，以及下次怎样避免”。

模型使用的最小化个人上下文包括：

- 本次真实误选项
- 本题累计错误次数
- 当前弱点类型
- 最近同类错误数量
- 最近同类考点，最多三个

模型必须返回固定 JSON 结构：

- `diagnosisSummary`：错误诊断
- `whyYouChoseIt`：为什么会选择该干扰项
- `reasoningSteps`：正好三步的判断路径
- `confusionComparison`：正确考点与误选项的决定性差异
- `reviewPlan`：当前、明天、三天后的复习动作
- `transferQuestion`：一道同考点迁移题

服务端不会直接相信模型返回的事实，而是重新写入关键字段：

```text
correctPoint     <- 本地知识库
confusedPoint    <- 用户真实误选
wrongCount       <- 本地学习记录
weaknessType     <- 本地分类结果
testedPoint      <- 本地知识库
```

这种设计可以总结为：

> 模型生成软内容，程序控制硬事实。

### 3.3 AI 迁移题与学习闭环

个性化辅导不仅输出解释，还会生成一道新的同考点选择题，用来验证用户是否真正掌握。

```text
原始错题
  → AI 分析错误原因
  → 生成同考点迁移题
  → 服务端校验结构和事实边界
  → 用户立即作答
  → 保存迁移结果
  → 更新个性化上下文版本
  → 影响后续弱点统计和辅导缓存
```

迁移题必须满足：

- 包含 2～4 个非空选项
- 选项不能重复
- 答案索引合法
- 继续考查原题锁定的 `testedPoint`
- 解释不能为空
- 判断路径必须正好三条

迁移结果最多保存最近 100 条，并进入跨模块弱点统计。

面试时可以强调：

> AI 输出不是流程终点。系统用新题验证用户是否真正理解，并将结果重新写回学习状态，形成“错误—解释—验证—再学习”的闭环。

### 3.4 上下文感知缓存

普通知识讲解基本只取决于题目，因此可以按 `questionId` 缓存。

个性化辅导取决于用户当前学习状态，缓存键为：

```text
questionId + contextVersion
```

`contextVersion` 包含：

- 当前错误次数
- 本次误选项
- 同类错误数量
- 最近迁移题是通过、失败还是尚未作答

用户再次答错或迁移结果发生变化后，上下文版本随之变化，旧的个性化分析不会继续被错误复用。

面试表达：

> 我没有只做简单的 Prompt-Response 缓存，而是根据学习上下文做版本化缓存失效，在成本、延迟和个性化新鲜度之间取平衡。

### 3.5 文法排序题的 AI 解题路径

文法排序模块可以生成结构化解题过程：

1. 锁定句尾。
2. 识别固定语法块。
3. 分析助词连接。
4. 确定完整顺序。
5. 总结一条可迁移规则。

Prompt 要求模型必须引用当前题目的真实日语碎片，不能只输出“先看语法、再排句子”这类空泛建议，并强制返回五字段 JSON。

该功能更接近受约束的教学推导。面试时应称为“结构化解题路径”，不应声称系统暴露了模型内部思维链。

## 四、混合知识检索系统

FastAPI 服务构建了 886 条可检索文档：

- 800 道文法与词汇题
- 60 道读解题
- 26 道听力题

### 4.1 检索流程

```text
中文或日语查询
  ├─ BM25 词项召回
  ├─ 本地 TF-IDF 余弦相似度
  ├─ 可选语义 Embedding
  └─ 考点、标签、完整短语规则加分
            ↓
        分数归一化
            ↓
        确定性重排
            ↓
     返回 Top-K 与命中原因
```

启用 Embedding 时的融合权重为：

```text
0.40 × BM25
+ 0.20 × TF-IDF
+ 0.30 × Semantic Embedding
+ 0.10 × Rule Bonus
+ Exact Tested-Point Bonus
```

未启用 Embedding 时为：

```text
0.55 × BM25
+ 0.35 × TF-IDF
+ 0.10 × Rule Bonus
+ Exact Tested-Point Bonus
```

### 4.2 为什么采用混合检索

- 日语语法名称和题目关键词适合 BM25 精确召回。
- 中文学习意图和局部语义适合 TF-IDF。
- 表面词不同但含义相近的查询适合 Embedding。
- 考点和标签属于高价值业务字段，适合确定性规则加权。
- 返回每个分项得分和 `matchReasons`，便于解释排序结果。

### 4.3 Embedding 缓存与回退

可选 Embedding 不会在每次启动时重新外发整个题库。系统会校验：

- Embedding 模型名称
- 题库内容指纹
- 文档 ID 集合
- 向量维度
- 向量数值合法性

只有缓存、模型和题库指纹完全匹配时，查询才会使用语义向量。缓存失效、超时或服务异常时，系统自动回退到本地 BM25 和 TF-IDF。

这使 Embedding 成为增强能力，而不是系统的单点依赖。

## 五、读解与听力证据增强

### 5.1 读解证据定位

读解检索结果除了题目，还会返回：

- 文章 ID 和标题
- 原文段落编号
- 对应的真实段落文本
- 题库证据
- 从证据中提取的引用片段

系统会根据引用内容反查原文位置，而不是完全相信人工填写的段号，从而提高证据可追溯性。

### 5.2 听力证据分类

听力结果会返回：

- 场景
- 对话内容
- 作答依据
- 关键信号
- 陷阱点
- 听题检查清单

系统明确区分四种证据：

- `audio_transcript`：官方音频转写
- `dialogue_quote`：文字对话原句
- `stimulus_response`：即时应答刺激与回应
- `pedagogical_summary`：教学性总结

这样可以避免把模拟对话或教学翻译错误宣传成官方音频逐字稿。

### 5.3 当前产品边界

读解、听力检索和证据结构已经进入 FastAPI 接口及自动化测试，但 App 内暂时还没有完整的通用知识搜索页面。面试中可以把它描述为“已实现服务端 AI 平台能力，前端产品化入口待补充”。

## 六、受控 Web RAG

项目没有让模型在用户请求期间实时搜索整个互联网，而是采用“审批来源目录 + 显式离线同步 + 本地缓存检索”。

```text
审批过的官方网站
  → 显式运行同步脚本
  → HTML 安全校验
  → 清洗正文
  → 保存访问时间与内容哈希
  → 普通请求只查询本地网页缓存
```

### 6.1 安全和质量控制

- 只允许 HTTPS。
- 主机必须位于白名单中。
- 拒绝带账号密码的 URL。
- 拒绝私网 IP 和危险重定向。
- 只接受 HTML。
- 单次响应限制为 1 MB。
- 清除脚本、样式和常见 Prompt Injection 文本。
- 校验缓存内容哈希和有效期。
- 网络来源只能补充，不能覆盖本地事实。
- 模型只能引用服务端提供的来源 ID。

### 6.2 Web RAG 触发条件

只有以下条件同时成立时，系统才会返回网络补充：

1. 请求显式传入 `allowWeb: true`。
2. 本地 BM25、TF-IDF 或 Embedding 证据不足。
3. Web RAG 已显式启用。
4. 审批缓存存在且没有过期或被篡改。
5. 查询与网页缓存确实存在匹配。

本地证据充分时，Web 数据不会参与回答。

如果模型伪造来源 ID、返回格式错误或不可用，系统会降级为直接摘录已有证据；本地与网页都没有证据时返回 404，不让模型自行补全。

### 6.3 为什么不做开放实时搜索

考试讲解更看重准确性、稳定性和结果可复现。开放实时搜索会带来：

- 来源质量不一致
- Prompt Injection
- SSRF 和危险重定向
- 版权及授权问题
- 网络超时和结果变化
- 相互冲突的知识内容
- 难以建立稳定测试集

因此，该项目选择了更适合教育场景的受控补充模式。

## 七、稳定性与失败回退

### 7.1 错题讲解降级链路

```text
先构造可靠的本地知识库解释
        ↓
是否配置 FastAPI？
  是 → 调用服务端模型
        ↓ 失败或只返回本地内容
是否配置 Web 代理？
  是 → 使用代理润色本地证据
        ↓ 失败
返回本地知识库解释
```

如果连本地知识映射都不存在，系统直接拒答，而不是把无证据题目发送给模型自由回答。

### 7.2 个性化辅导降级

个性化 Tutor 对生成模型依赖更强。模型不可用或结构校验失败时，系统不会伪造个性化结果，而是保留知识库事实讲解，并提示个性化服务暂时不可用。

### 7.3 其他可靠性措施

- 前端请求超时为 55 秒。
- 服务端模型请求超时为 45 秒。
- 清理 Markdown JSON Fence。
- 使用 Pydantic 响应模型。
- 校验字段类型、数组长度和答案索引。
- 缓存 AI 讲解和个性化辅导。
- 学习上下文变化后使缓存失效。
- `/health` 检查服务、索引与配置。
- `/health/ai` 使用不包含题库数据的固定探针检查真实模型连接。

## 八、确定性弱点分析与生成式 AI 的边界

首页会聚合文法、词汇、读解和听力的错误数据，计算：

- 当前最严重的弱点类型
- 错误暴露次数
- 弱点变化趋势
- 推荐训练模式
- 迁移题正确率
- 尚需重试的问题数量

这部分使用标签映射、规则判断和统计排序，不是 LLM，也不是机器学习模型。

推荐的面试说法：

> 系统同时使用确定性学习分析和生成式 AI。弱点识别与训练推荐优先采用可解释规则；只有需要自然语言诊断、对比解释和迁移题生成时才调用模型。这样可以降低成本，并使关键业务决策保持稳定和可测试。

## 九、AI 质量评估体系

项目提供 40 个固定个性化辅导案例：

- 文法 20 个
- 词汇 20 个
- 同一道题覆盖首次错误和第四次重复错误两种学习上下文

### 9.1 评估指标

- 模型成功率和回退率
- 结构校验失败率
- 事实锁定字段正确率
- 是否使用了真实个性化信息
- 平均延迟、P50 和 P95
- Prompt Token 和 Completion Token
- 根据模型单价估算的成本
- 迁移题质量分

### 9.2 迁移题质量规则

迁移题质量分由六项可复现规则构成：

1. 保持相同考点。
2. 题干不是原题复制。
3. 选项没有重复。
4. 答案索引合法。
5. 解释非空。
6. 题目或解释包含考点锚点。

### 9.3 当前验证状态

截至 2026-08-27，本地验证结果为：

- TypeScript 和领域层测试：50/50 通过
- Python AI 服务测试：41/41 通过
- `npx tsc --noEmit`：通过

这些测试覆盖：

- 事实字段锁定
- 无知识证据拒答
- JSON 结构校验
- 个性化上下文版本变化
- 混合检索与稳定排序
- Embedding 不可用时回退
- 读解证据定位
- 听力证据分类
- Web 来源白名单
- 私网和危险 URL 拒绝
- 网页缓存完整性与时效校验
- 引用 ID 白名单
- Token、成本与延迟统计

需要注意：仓库没有保存真实模型完整运行 40 个案例后的最新报告。因此面试时应该说“已经建立可复现的离线评估框架”，不应声称已经获得某个未经保存的线上准确率。

## 十、最值得强调的三个技术亮点

### 10.1 事实层与生成层分离

答案和来源由知识库锁定，模型只生成允许变化的教学文本。即使模型越权输出，程序也只合并白名单字段。

这体现了对 LLM 幻觉、输出不稳定和生产可靠性的理解。

### 10.2 AI 输出形成学习闭环

AI 不只生成一段解释，还会生成迁移题。用户作答结果写回学习状态、弱点统计和缓存版本。

这体现了产品设计能力，而不仅是 Prompt Engineering。

### 10.3 完整的评估和回退机制

模型、Embedding 和网络资料都不是系统的单点依赖。任意一层不可用时，都可以退回确定性能力，并且有固定数据集评估结构、事实、延迟和成本。

这体现的是 AI Engineering 能力，而不仅是模型 API 接入能力。

## 十一、面试常见追问与参考回答

### 11.1 为什么不用大模型直接回答？

JLPT 存在大量近义语法和细粒度语境差异，模型容易生成听起来合理但事实错误的解释。系统先锁定题库证据，让模型负责组织教学表达，从而提高稳定性、可测试性和可追溯性。

### 11.2 你的 RAG 和普通向量数据库有什么区别？

这是一个轻量级混合 RAG。当前只有 886 条知识文档，因此没有为了技术名词强行引入向量数据库，而是使用 BM25、TF-IDF、可选 Embedding 和业务规则重排。这样部署更轻、可解释性更好。数据规模扩大后，可以迁移到 pgvector、Qdrant 或 Elasticsearch。

### 11.3 如何防止模型修改正确答案？

系统采用三层控制：

1. Prompt 明确限制模型职责。
2. 客户端和服务端只读取允许生成的字段。
3. 正确考点、误选项、错误次数、来源和迁移题考点由程序重新写入。

Prompt 是软约束，Schema、字段白名单和服务端覆盖才是硬约束。

### 11.4 如何证明个性化不是把几个字段拼进 Prompt？

个性化上下文来自真实学习记录；缓存键会随错误次数、误选项、同类错误和迁移结果变化；输出必须引用真实误选项；迁移结果还会回写系统并影响后续辅导。因此个性化不仅发生在 Prompt 输入阶段，也进入了状态管理和学习反馈闭环。

### 11.5 为什么使用 BM25、TF-IDF 和 Embedding 三种信号？

BM25 擅长精确词项匹配，TF-IDF 可以处理局部文本相似，Embedding 可以召回没有明显关键词重叠的语义相关内容。再结合考点和标签规则，可以兼顾准确召回、语义召回和业务优先级。

### 11.6 为什么 Web RAG 不做实时开放搜索？

实时搜索会增加来源质量、Prompt Injection、SSRF、版权、延迟和结果不可复现等问题。项目使用白名单来源、显式同步和带哈希的本地缓存，更适合考试讲解这种高准确性场景。

### 11.7 模型返回了非法 JSON 怎么办？

先清理常见 Markdown JSON Fence，再进行 JSON 解析和 Schema 校验。错题讲解失败时回退到本地知识库；个性化 Tutor 校验失败时返回服务不可用，不展示半成品。

### 11.8 为什么没有直接上向量数据库？

当前数据只有 886 条，使用内存索引和本地缓存足够，同时便于理解和展示完整算法。过早引入向量数据库会增加部署和维护复杂度。后续数据量、并发量和增量更新需求增加后，再迁移到专业存储。

### 11.9 如何控制模型成本？

- 只发送生成需要的最小学习上下文。
- 普通弱点统计不调用模型。
- 本地证据充分时不使用 Web 补充。
- Embedding 索引离线批量构建。
- 使用上下文版本缓存个性化结果。
- 评估器统计 Prompt、Completion Token 和估算成本。

### 11.10 当前最大的不足是什么？

可以诚实回答：

- 知识搜索和 Web RAG 主要位于服务端，前端通用搜索页面尚未完成。
- 迁移题目前主要进行结构和规则锚点校验，尚未实现强语义审校。
- 自动化测试验证了工程机制，但仍缺真实模型长期评估报告和真实用户效果数据。
- FastAPI 使用同步网络调用，生产环境应增加异步客户端、限流、鉴权和完善的可观测性。
- 当前 CORS 范围较宽；直接使用 `EXPO_PUBLIC_AI_API_KEY` 的路径也不适合正式生产，生产环境应统一通过服务端代理管理密钥。
- 当前检索规模较小，尚未验证大规模索引、增量更新和多用户并发。

## 十二、60 秒面试讲解稿

> 这是一个 JLPT N2 AI 错题辅导项目，前端使用 React Native 和 Expo，AI 服务使用 FastAPI。项目的核心不是简单调用大模型，而是建立可靠的 RAG 和个性化学习闭环。
>
> 用户做错题后，系统先根据 questionId 从结构化题库锁定正确答案、考点、选项分析和来源，然后只允许大模型生成错误诊断和教学表达，程序会用字段白名单和 Pydantic Schema 防止模型修改事实。
>
> 个性化辅导会结合用户的真实误选项、累计错误次数和同类错误，生成三步判断路径、易混点对比、复习计划和一道同考点迁移题。迁移结果会写回学习状态，并触发个性化缓存换版。
>
> 后端还实现了覆盖 886 条内容的 BM25、TF-IDF、可选 Embedding 混合检索，以及白名单、离线同步、哈希校验的受控 Web RAG。模型、Embedding 或网络资料不可用时，系统都会回退到本地知识库。
>
> 最后我建立了 40 个固定案例的评估框架，用于统计事实锁定、个性化有效性、迁移题质量、延迟、Token 和成本。目前 50 项前端领域测试和 41 项 AI 服务测试全部通过。

## 十三、三分钟展开版讲解顺序

如果面试官允许进一步展开，可以按照下面的顺序介绍：

1. 先说明业务问题：直接使用 LLM 解释日语近义语法容易产生幻觉。
2. 介绍事实与生成分层：知识库锁定答案，LLM 负责表达。
3. 介绍个性化输入：误选项、错误次数、弱点和同类错误。
4. 介绍迁移题闭环：生成新题、校验、作答、结果回写。
5. 介绍混合检索：BM25、TF-IDF、Embedding 和规则重排。
6. 介绍 Web RAG：白名单、离线同步、哈希与时效校验。
7. 介绍可靠性：结构校验、拒答、缓存、超时和回退。
8. 介绍评估体系和当前不足。

## 十四、简历项目描述

### JLPT N2 个性化 AI Tutor 与 RAG 检索系统

- 基于 React Native、TypeScript 与 FastAPI 构建 AI 学习应用，实现错题讲解、个性化错误诊断、三步判断路径、复习建议及同考点迁移验证。
- 设计“知识库锁定事实、LLM 生成表达”的受控生成架构，通过字段白名单、Pydantic Schema、引用 ID 校验、证据不足拒答和本地降级降低模型幻觉风险。
- 针对 886 条中日双语学习内容实现 BM25、TF-IDF、可选 Embedding 与业务规则融合的混合检索，并返回分项得分和可解释命中原因。
- 实现审批来源目录、HTTPS/主机/IP 校验、HTML 清洗、内容哈希和有效期校验的受控 Web RAG，确保网络资料只能补充、不能覆盖本地事实。
- 根据误选项、错误次数、同类弱点和迁移结果构建版本化个性化缓存，将 AI 生成结果重新接入学习进度闭环。
- 建立 40 案例 AI 离线评估框架，覆盖事实锁定、结构失败、迁移题质量、延迟、Token 与模型成本；当前 91 项自动化测试全部通过。

## 十五、核心代码入口

- 前端 AI 客户端：`src/services/aiCoachClient.ts`
- 本地 RAG 讲解：`src/domain/services/ragExplanationService.ts`
- 个性化上下文与缓存：`src/domain/services/personalizedTutorService.ts`
- 弱点分类：`src/domain/services/wrongAnswerClassifier.ts`
- 跨模块弱点分析：`src/domain/services/coachService.ts`
- 错题 AI 界面：`src/features/wrong-review/screens/WrongReviewScreen.tsx`
- 文法排序 AI 界面：`src/features/grammar-200/screens/Grammar200Screen.tsx`
- FastAPI 接口：`ai-service/app/main.py`
- LLM Gateway：`ai-service/app/llm_gateway.py`
- 混合检索：`ai-service/app/retrieval_service.py`
- Embedding：`ai-service/app/embedding_service.py`
- 受控 Web RAG：`ai-service/app/controlled_web_service.py`
- 知识研究回答：`ai-service/app/research_service.py`
- 离线评估：`ai-service/app/evaluation.py`

## 十六、面试表述注意事项

为了保证技术表述准确，建议遵守以下边界：

- 可以说“实现了轻量级混合 RAG”，但不要说每一次错题解释都执行了向量检索；错题链路主要是精确知识映射。
- 可以说“实现了个性化 AI Tutor”，但不要说弱点分类本身使用了机器学习；当前分类和推荐主要是确定性规则。
- 可以说“建立了 40 案例评估框架”，但不要编造未经真实模型完整运行并保存的准确率。
- 可以说“支持受控 Web RAG”，同时说明它使用审批缓存，不是开放式实时搜索引擎。
- 可以说“生成结构化解题路径”，不要把它描述成暴露模型内部 Chain of Thought。
- 可以说“具备生产意识和可靠性设计”，但也应主动说明鉴权、限流、异步化、观测和真实用户验证仍是后续工程化工作。
