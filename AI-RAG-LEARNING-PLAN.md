# JLPT N2 AI RAG 学习平台方案

本文档是 2026-08-06 确认后的阶段性方案基线。目标不是推翻已有 app，而是在现有文法、词汇、读解、听力、错题回收和学习进度基础上，扩展一个小而精、链路完整、适合学习和求职展示的 AI 应用能力。

> 实施状态（2026-08-14）：`AI-M0`、`AI-M1` 与 `AI-M2` 已落地。800 道文法/词汇题具备精确知识映射；错题页已将知识库事实讲解与 AI 个性化辅导分离，并支持诊断、三步判断路径、复习动作、迁移题、结果记录和上下文缓存换版。AI-M2 新增 40 案例固定评估集及回退率、结构失败率、响应耗时、迁移题质量、token 和成本统计；真实用户迁移正确率对照、BM25、向量检索和 Rerank 属于后续阶段。详细设计见 [个性化 AI 错题辅导设计](./AI-PERSONALIZED-TUTOR-DESIGN.md)。

## 项目定位

项目名称建议：

```text
JLPT N2 AI 错题讲解与知识检索系统
```

核心定位：

```text
练习模块负责产生学习数据，AI RAG 层负责基于知识库解释错题、引用来源、分析薄弱点，并给出复习建议。
```

这不是单独新做一个聊天机器人，也不是把原来的练习模块废掉。原有成果继续保留，并作为 AI 能力的业务底座。

## 保留的原有模块

以下模块继续作为产品主体：

- 文法刷题与文法学习包
- 词汇刷题、词汇学习包与官方词卡记忆
- 读解训练与弱点回收
- 听力训练与陷阱识别
- 错题记录与错题回收
- 首页薄弱点摘要、训练推荐和学习进度

AI 层只做增强：

```text
词汇 / 文法 / 读解 / 听力练习
  -> 产生错题和弱点数据
  -> AI 检索知识库
  -> 生成可追溯讲解
  -> 反向沉淀为复习建议
```

## 第一阶段目标

第一阶段做一个小规模但完整的 AI 功能闭环：

1. 用户做错一道题。
2. 系统读取题目、用户答案、正确答案和已有解析。
3. 系统根据题目的知识点，从结构化知识库中查资料。
4. 系统使用关键词检索和向量检索补充相近资料。
5. 系统通过 rerank 选择最相关资料。
6. 大模型只基于检索资料生成讲解。
7. 前端展示正确答案、考点、错误原因、选项分析和来源引用。
8. 系统保存 AI 讲解记录，并进入后续薄弱点统计。

第一阶段优先覆盖：

1. 文法错题 AI 讲解
2. 词汇错题 AI 讲解
3. 读解错题 AI 定位分析
4. 听力错题 AI 陷阱分析

文法和词汇最结构化，优先做。读解和听力需要原文、定位句、音频转写和干扰项分析，放到后续增强。

## 总体架构

建议架构：

```text
React Native / Expo 前端
        |
Node.js / Express 业务后端
        |
Python FastAPI AI 服务
        |
知识库层
  - JSON / SQLite
  - BM25 关键词检索
  - Chroma 向量检索
  - Rerank
        |
LLM API
```

职责划分：

- React Native：学习体验、题目作答、AI 讲解展示。
- Node.js：业务 API、用户数据、错题记录、学习进度。
- Python FastAPI：AI 能力边界，包括检索、rerank、Prompt 构造和 LLM 调用。
- 知识库层：保存语法、词汇、读解、听力和题目解析的可追溯资料。
- LLM API：根据检索资料生成结构化讲解。

## 为什么不是直接调大模型

直接把错题丢给大模型有两个问题：

- 它可能编造语法规则或解释。
- 它很难稳定区分 JLPT 里相近但不等价的表达。

RAG 的价值是：

```text
先查资料，再回答。
```

也就是：

```text
用户错题
  -> 检索知识库
  -> 选出可信资料
  -> 大模型基于资料组织讲解
```

这样可以降低幻觉，并且让用户看到解释来源。

## 为什么第一阶段不直接上网查资料

RAG 不等于互联网搜索。RAG 的核心是“先检索可信资料，再让大模型基于检索结果回答”，资料既可以来自项目自己的知识库，也可以来自互联网。

本项目第一阶段优先使用经过整理和校对的本地知识库，原因是 JLPT 错题讲解更看重准确、稳定和可追溯：

- 搜索引擎返回的博客、论坛和聚合内容质量不一致，解释可能错误或互相冲突。
- 搜索结果可能混入 N1、N3 或非考试语境内容，导致语法和词义判断偏离 N2 场景。
- 网页可能更新、删除或限制访问，同一道题的讲解结果难以稳定复现和测试。
- 网页中的广告、无关内容和恶意提示文本需要额外清洗与安全处理。
- 直接使用网络内容还需要处理版权、来源许可、请求成本和响应超时等问题。

因此，第一阶段采用“封闭知识库 RAG”：

```text
题目
  -> 题目与知识点精确映射
  -> 自建语法 / 词汇 / 读解 / 听力知识库
  -> BM25 + 向量检索 + Rerank
  -> LLM 基于证据生成讲解
  -> 展示本地资料来源
```

这样更容易建立测试集，评估检索命中率和讲解准确性，也更适合做一个稳定、可演示的求职项目。

### 后续扩展：受控 Web RAG

联网检索可以作为后续增强，但采用“受控补充”模式，而不是无限制搜索整个互联网：

```text
优先查询本地可信知识库
  -> 证据不足或没有命中
  -> 查询来源白名单中的官方网站或授权资料
  -> 抽取、清洗和校验内容
  -> 明确标记网络来源和访问时间
  -> LLM 基于本地与网络证据生成回答
```

建议的控制规则：

- 优先允许 JLPT 官方网站、官方样题和已确认许可的学习资料。
- 网络资料必须保留标题、URL、发布时间或访问时间等来源信息。
- 网络资料与本地知识库冲突时，不自动覆盖本地条目，应标记冲突并等待人工确认。
- 搜索失败、超时或证据可信度不足时，返回“资料不足”，不让模型自行补全。
- 将“本地知识库回答”和“网络补充回答”在界面上明确区分。

受控 Web RAG 属于第一阶段完成后的扩展亮点，不是 MVP 的前置条件。

## 核心功能范围

### 1. AI 错题讲解

`AI-M0` 的知识库讲解负责回答“这道题为什么这样选”。下一阶段的个性化 AI 辅导负责回答“你为什么会错、下次怎样避免”，两者不能继续使用完全相同的页面结构。

输入示例：

```json
{
  "questionId": "grammar-001",
  "userAnswer": "B"
}
```

输出示例：

```json
{
  "correctAnswer": "A",
  "testedPoint": "に伴って",
  "whyCorrect": "「に伴って」表示随着某个变化，另一个变化也同时发生。",
  "whyUserWrong": "「に対して」表示对象、对比或针对，不表示伴随变化。",
  "choiceAnalysis": [
    {
      "choice": "A",
      "reason": "正确，表示随着台风接近，交通受到影响。"
    },
    {
      "choice": "B",
      "reason": "错误，语义是对于或相对，不符合变化伴随关系。"
    }
  ],
  "sources": [
    {
      "id": "grammar_n2_001",
      "title": "に伴って",
      "snippet": "表示随着某个变化，另一个变化也一起发生。"
    }
  ],
  "confidence": "high"
}
```

个性化辅导将在上述事实输出之外新增：

- 基于真实误选项和累计错误次数的错误诊断。
- 可迁移到同类题的三步判断路径。
- 正确考点与误选考点的针对性对比。
- 当前、明天和三天后的复习动作。
- 一道受约束的迁移题，用于验证用户是否真正理解。

答案、考点、选项事实和来源继续由知识库锁定，模型不得改写。字段设计、接口、缓存和评估方案见 [个性化 AI 错题辅导设计](./AI-PERSONALIZED-TUTOR-DESIGN.md)。

页面命名上，`ai_service` 和 `ai_tutor` 保留为内部枚举，不直接作为用户标题。模型讲解成功时不增加“AI 增强”徽标；降级来源显示“本地知识库/历史缓存”，个性化区域显示“智能辅导”。

### 2. 语法和词汇知识检索

支持用户问：

```text
「に伴って」と「につれて」の違いは？
```

系统返回基于知识库的对比说明，并展示来源。

### 3. 阅读错题定位分析

读解题答错后，AI 需要说明：

- 原文依据在哪一段。
- 正确选项为什么符合原文。
- 用户选择的干扰项错在哪里。
- 文章逻辑应该如何抓。

### 4. 听力陷阱分析

听力题答错后，AI 需要说明：

- 听力原文中的关键信息。
- 用户可能漏听的转折、否定、最终决定或条件。
- 干扰项为什么容易误选。
- 下次听同类题时先抓什么。

### 5. 错题本复习建议

系统根据错题记录总结：

```text
你最近错得较多的是：
1. 变化伴随类语法：に伴って、につれて、にしたがって
2. 对象关系类语法：に対して、について

建议先复习“变化伴随”这一组，再做 5 道相近语法辨析题。
```

## 知识库设计

第一阶段不要先做 PDF 自动解析。先手工整理一批高质量结构化数据，数量小但质量高。

建议初始规模：

- N2 语法点：30 个
- N2 词汇：50 个
- 练习题：50 道
- 读解文章：选 3-5 篇做 AI 定位增强
- 听力案例：选 3-5 条做 AI 陷阱增强

### 语法知识条目

```json
{
  "id": "grammar_n2_001",
  "level": "N2",
  "pattern": "に伴って",
  "meaningZh": "随着……，伴随着……",
  "usage": [
    "名词 + に伴って",
    "动词辞书形 + に伴って"
  ],
  "examples": [
    {
      "jp": "人口の増加に伴って、住宅問題が深刻になった。",
      "zh": "随着人口增加，住房问题变得严重。"
    }
  ],
  "similarPatterns": ["につれて", "にしたがって", "とともに"],
  "notes": "多用于书面语，表示一个变化伴随另一个变化。",
  "source": "manual_seed"
}
```

### 词汇知识条目

```json
{
  "id": "vocab_n2_001",
  "level": "N2",
  "word": "影響",
  "reading": "えいきょう",
  "meaningZh": "影响",
  "usage": "影響を受ける / 影響を与える",
  "examples": [
    {
      "jp": "台風の影響で電車が遅れた。",
      "zh": "受台风影响，电车晚点了。"
    }
  ],
  "similarWords": ["効果", "結果"],
  "source": "manual_seed"
}
```

### 题目知识映射

```json
{
  "id": "grammar-001",
  "type": "grammar",
  "level": "N2",
  "stem": "台風の接近___、交通機関に影響が出ています。",
  "choices": [
    {"label": "A", "text": "に伴って"},
    {"label": "B", "text": "に対して"},
    {"label": "C", "text": "によって"},
    {"label": "D", "text": "について"}
  ],
  "answer": "A",
  "testedPointId": "grammar_n2_001",
  "distractorPointIds": ["grammar_n2_002", "grammar_n2_003", "grammar_n2_004"],
  "difficulty": "medium"
}
```

这个映射非常关键。它让系统可以先稳定查到考点，再用 RAG 补充资料，而不是让 AI 猜题目考什么。

## RAG 检索链路

第一阶段建议使用三层检索：

### 第 1 层：结构化精确查找

```text
questionId -> testedPointId -> grammar/vocab item
```

这是最稳定的通道，适合错题讲解。

### 第 2 层：BM25 关键词检索

适合查固定表达、题号、单词和语法名：

```text
に伴って
使役受身
影響
grammar-001
```

### 第 3 层：向量检索

适合查自然语言问题：

```text
表示随着变化的语法
容易和 によって 混淆的表达
表示原因和根据的句型
```

完整流程：

```text
用户错题
  |
读取题目和用户答案
  |
精确找到 testedPoint
  |
BM25 搜相近语法 / 词汇
  |
向量检索补充相关资料
  |
Rerank 排序
  |
选 top 3-5 条资料
  |
构造 Prompt
  |
LLM 生成结构化讲解
  |
保存讲解记录
```

## 技术选型

第一阶段建议：

| 层 | 推荐选型 | 理由 |
| --- | --- | --- |
| 前端 | React Native / Expo | 复用现有 app |
| 业务后端 | Node.js / Express | 承接用户、错题、进度等业务 API |
| AI 服务 | Python FastAPI | AI 生态成熟，接口简单 |
| 结构化存储 | JSON / SQLite | 小规模、易维护、易调试 |
| 关键词检索 | SQLite FTS5 或 rank-bm25 | 精确表达检索稳定 |
| 向量库 | Chroma | 本地轻量，适合 MVP |
| Embedding | bge-m3 或 OpenAI embedding | 支持中日混合语义 |
| Rerank | bge-reranker 或 Cohere Rerank | 提升召回结果质量 |
| LLM | OpenAI / Qwen / DeepSeek API | 先用 API，不做本地部署 |

暂时不做：

- PDF 自动解析
- 全网开放式搜索；后续仅考虑有来源白名单和失败回退的受控 Web RAG
- 复杂 Agent
- 企业级向量数据库
- 本地大模型部署
- 多用户商业化系统
- 复杂学习路径规划

个性化错题诊断和单题迁移验证不属于“复杂学习路径规划”，是当前 RAG 闭环的下一步增强。

## 从 AI-M0 出发的 4 周执行计划

### 第 1 周：个性化诊断

- 新增 `POST /tutor/wrong-answer` Schema 和服务。
- 汇总误选项、错误次数和最小化同类错误历史。
- 生成错误诊断、三步判断路径和易混点对比。
- 将页面拆分为“知识库事实”和“AI 个性化辅导”。

### 第 2 周：迁移验证

- 生成受约束的同考点迁移题。
- 校验选项数量、答案索引、考点和解释依据。
- 新增 `POST /tutor/transfer-result` 并保存验证结果。
- 使用学习上下文版本控制个性化缓存失效。

### 第 3 周：质量评估

- 建立 30-50 道固定评估集和两组用户上下文。
- 统计字段校验通过率、模型回退率、响应耗时和成本。
- 对比只看知识库讲解与使用 AI 辅导后的迁移正确率。
- 修正空泛诊断、未引用真实误选项和不可执行建议。

### 第 4 周：检索增强与展示

- 为相似语法和自然语言问题加入 BM25，再评估向量检索与 Rerank。
- 完善架构图、演示数据、README 和面试说明。
- 演示同一道题在不同错误历史下产生不同辅导，但事实层完全一致。

## 简历表达

当前 `AI-M0` 已完成后可以写：

```text
设计并实现 JLPT N2 结构化 RAG 错题讲解系统，基于 FastAPI 构建独立 AI 服务，为 800 道文法和词汇题建立精确知识映射。系统通过字段白名单锁定答案、考点和来源，支持结构化讲解、来源引用、资料不足拒答、缓存、本地回退和真实模型健康检查。
```

完成个性化诊断和迁移验证后可以写：

```text
在可追溯 RAG 事实层之上实现自适应 AI 辅导，结合用户误选项、重复错误和近期同类弱点生成个性化诊断、三步判断路径与受约束迁移题；通过 JSON Schema、事实字段锁定、失败回退和迁移正确率评估控制幻觉并验证实际学习效果。
```

## 面试讲解重点

面试时不要只讲“我用了大模型”，要讲清楚：

- 为什么不能直接让 LLM 讲题：会幻觉，尤其是语法辨析场景。
- 为什么要结构化知识库：题目、语法、词汇之间有明确关系，结构化比纯文本切块更稳定。
- 为什么要混合检索：关键词检索适合固定表达，向量检索适合自然语言问题。
- 为什么要 Rerank：初次召回结果不一定都适合，需要二次排序提高上下文质量。
- 怎么控制输出质量：来源引用、Prompt 约束、JSON Schema、资料不足时拒答、测试集评估。

## 成功标准

第一阶段完成后，项目应能演示：

- 原有文法、词汇、读解、听力练习仍可正常使用。
- 文法错题可生成结构化 AI 讲解。
- 讲解包含正确原因、错误选项原因和来源引用。
- 知识库不足时不会编造解释。
- 有一套小规模测试集能验证检索和讲解质量。
- README 能清楚说明这是一个轻量 RAG 学习系统，而不是简单 API 套壳。
- 同一道题在不同误选项、错误次数和薄弱点上下文下，能生成明显不同且可解释的辅导方案。
- AI 讲解后可用迁移题验证学习效果，而不是只比较文案是否更流畅。
