# 下一阶段完善清单

更新日期：2026-09-20。

当前项目已经完成 `M0：可用` 和 `M1：有效`，正在完成 `M2：可信`。核心功能、Web 部署、自动化测试和移动构建配置已经具备；下一阶段的重点不是继续增加训练模式，而是完成真机交付、真实学习验证、内容与 AI 质量验收，以及用户数据保护。

## 当前基线

- Node 逻辑测试：69/69
- Python AI 测试：44/44
- Chromium 生产构建回归：16/16
- TypeScript、Web 生产导出、线上生产验收和移动配置验收通过
- Android `preview` APK 和两端 `production` EAS 档案已配置
- Web 正式版本已上线
- 尚未完成：EAS 项目关联、Android 预览包、移动真机验收、14 天真实效果验证和真实模型完整评估

## P0：Android 预览包与真机验收

目标：把“配置可以构建”推进到“真机可以稳定使用”。

### 构建任务

- [ ] 恢复稳定的 npm / Expo 网络连接。
- [ ] 登录 Expo/EAS 账号。
- [ ] 完成首次 EAS 项目关联，在 `app.json` 保存 `extra.eas.projectId`。
- [ ] 运行 `npm run test:mobile-config`，确认项目关联后配置仍然通过。
- [ ] 生成 Android Preview APK。
- [ ] 保存构建链接、构建编号和对应 Git 提交。

```bash
npx eas-cli login
npx eas-cli whoami
npx eas-cli build --platform android --profile preview
```

如果 EAS CLI 因本机网络不可用，可先运行 GitHub Actions 的 `Android Preview APK` 备用工作流生成内部测试包；该产物用于完成真机验收，后续仍需补做 EAS 项目关联和正式签名构建。

### 真机任务

- [ ] 冷启动、退到后台、返回前台和系统返回键正常。
- [ ] 首页及所有核心训练页面没有白屏、遮挡或横向溢出。
- [ ] 完成文法、词汇、读解和听力训练，重启应用后进度仍然存在。
- [ ] 验证错题、不稳项、到期任务和首页推荐能够形成闭环。
- [ ] 验证 5 个已有官方音频的播放、暂停、重复播放、外放与耳机行为。
- [ ] 确认系统没有请求麦克风、相册或文件写入等非必要权限。
- [ ] 在断网和弱网下完成本地训练；AI 不可用时仍显示本地结构化讲解。

完成标准：至少一台真实 Android 设备完整通过 [移动端构建与验收](./MOBILE_RELEASE.md)，发现的问题有记录、有复现步骤并完成修复或明确接受风险。

## P1：连续 14 天真实学习验证

目标：证明推荐和复习系统不仅能运行，而且能帮助减少重复错误。

### 每日记录

- [ ] 每天先从首页推荐进入训练，记录推荐是否符合当天直觉。
- [ ] 完成至少一个主要训练模式和当天到期复习。
- [ ] 记录推荐不合理、积压过多、重复内容过密或复习间隔不合适的情况。
- [ ] 对 AI 迁移题记录是否正确，以及是否真正检验了同一考点。
- [ ] 不手工篡改进度数据，保证错误趋势来自真实训练事件。

### 第 7 天与第 14 天复盘

- [ ] 检查重复错误数、到期复习量、已晋级项目和迁移题正确率。
- [ ] 第 14 天检查“最近 7 天”与“此前 7 天”的错误趋势。
- [ ] 抽查首页最优先的三项任务，确认其顺序可以解释。
- [ ] 汇总至少 5 个有效推荐案例和全部明显不合理案例。

完成标准：14 天数据完整，推荐顺序大多数时候符合学习需要，并能观察到重复错误稳定或下降；否则根据记录调整权重、间隔或结果页建议，再重新验证。

## P2：听力内容与授权音频

目标：让听力训练的“文字技巧内容”和“真实音频训练”边界清楚，逐步提高真实音频覆盖。

- [ ] 为新增音频确认来源、授权范围和可分发性。
- [ ] 音频文件、转写文本、题目、选项、答案和 `basisLine` 逐条对应。
- [ ] 在 `source` 或 `sourceHint` 中保留清晰来源信息。
- [ ] 对每个新增音频执行播放、暂停、重复播放和完整答题验收。
- [ ] 没有匹配音频的案例继续明确作为文字对话与解题技巧训练，不暗示为真实原题音频。
- [ ] 更新内容数量时同步修改 `README.md`、`CONTENT-PROGRESS.md` 和 `ROADMAP.md`。

完成标准：每个标记为音频训练的案例都有匹配且可合法分发的音频，自动内容校验和真机播放均通过。

## P3：真实 AI 模型质量验收

目标：用真实模型和真实价格建立可复现的 AI 发布基线。

- [ ] 在服务端配置模型地址、模型名和密钥，不把密钥写入前端公开变量。
- [ ] 填写真实输入、输出 token 单价。
- [ ] 先用 `--limit 2` 做连通和费用冒烟测试。
- [ ] 运行全部 40 个固定案例并保存报告。
- [ ] 检查回退率、结构失败率、事实锁定、个性化、迁移题质量、P95 延迟和总成本。
- [ ] 分别检查首次错误和重复错误两个上下文分组。
- [ ] 根据首次完整报告确定发布阈值，此后用相同门槛比较模型或提示词版本。

```bash
AI_LLM_INPUT_COST_PER_MILLION=<实际价格> \
AI_LLM_OUTPUT_COST_PER_MILLION=<实际价格> \
npm run ai:evaluate -- --output evaluation/latest-report.json
```

完成标准：40 个案例全部运行，报告包含模型、价格、评估集指纹和失败原因；事实锁定不能被模型覆盖，成本与延迟处于可接受范围。

## P4：学习数据导出与安全恢复

目标：在账号和云同步之前，先让用户能够保护本地学习记录。

- [ ] 定义带 `schemaVersion`、导出时间和应用版本的备份格式。
- [ ] 支持将学习进度导出为 JSON。
- [ ] 导入前完成 JSON、版本、字段和数据范围校验。
- [ ] 导入前展示摘要并要求用户确认，不静默覆盖现有数据。
- [ ] 导入失败时保持原数据不变，并提供明确错误提示。
- [ ] 覆盖空数据、旧版本、损坏文件、超大文件和重复导入测试。
- [ ] 真机验证 Android 文件分享/选择流程，不申请不必要的存储权限。

完成标准：用户可以可靠备份并恢复全部学习进度，错误文件不会破坏现有状态。账号、后台和跨设备同步仍放在实际多人需求出现后评估。

## P5：商店发布准备

这部分在 Android Preview 真机验收和 14 天效果验证之后开始。

- [ ] 准备隐私政策 URL、应用介绍、图标、手机截图和更新说明。
- [ ] 完成 Google Play 内容分级、目标受众、广告和数据安全声明。
- [ ] 说明学习进度存储位置、AI 请求范围及第三方服务。
- [ ] 检查正式包中没有模型密钥、测试地址、调试日志或个人数据。
- [ ] 确认正式 AI 代理的 HTTPS、CORS、限流、错误回退和成本控制。
- [ ] 建立版本号、构建号、发布标签和回滚记录规则。
- [ ] Android 稳定后，再准备 Apple Developer、App Store Connect、iOS 签名和隐私资料。

完成标准：正式构建通过自动验收和真机清单，商店资料完整，隐私与 AI 数据流描述和实际实现一致。

## 暂不优先

在上述项目完成前，暂不优先增加新训练模式、复杂 Agent、企业级向量数据库、账号后台或多端实时同步。任何新需求都应先回答：它是否能直接增强“识别薄弱点 → 推荐下一步 → 回收验证 → 减少重复错误”的闭环。

## 推荐执行顺序

1. Android Preview APK 与真机验收。
2. 开始 14 天真实使用；验证期间可并行整理授权听力音频。
3. 运行真实模型 40 案例评估并确定 AI 发布门槛。
4. 实现学习数据导出与安全恢复。
5. 根据前四项结果准备 Android 商店发布；iOS 随后推进。
