# 读解与听力 AI 个性化辅导完整设计

> 状态：MVP 已实现，跨题迁移与真实模型质量验收待继续
>
> 更新日期：2026-09-24
>
> 依赖基线：[个性化 AI 错题辅导设计](./AI-PERSONALIZED-TUTOR-DESIGN.md)、[AI RAG 学习平台方案](./AI-RAG-LEARNING-PLAN.md)

## 1. 文档目标

本文补齐读解与听力的 AI 个性化辅导设计，使两个模块都形成以下闭环：

```text
作答
  -> 固定证据解析
  -> 个性化错误诊断
  -> 可重复执行的解题路径
  -> 即时微验证
  -> 本地校对题迁移验证
  -> 结果写回弱点与复习调度
```

截至 2026-09-24，当前仓库中：

- 文法、词汇错题已经具备个性化辅导主链路。
- 读解已在答错后的固定解析下接入 AI 个性化辅导、缓存和即时验证。
- 听力已在原文、依据句、关键信号和陷阱分析后接入 AI 个性化辅导、缓存和即时验证。
- 两个模块的即时验证答案均由本地题库锁定，不采用模型自由生成的答案。
- 本地校对题跨题迁移、独立作答事件历史和真实模型长期质量验收仍属于下一阶段。

## 2. 设计原则

### 2.1 固定事实与 AI 职责分离

固定题库负责：

- 原文、音频与题干。
- 正确答案。
- 证据句或依据台词。
- 选项事实与干扰项机制。
- 题目标签、来源和弱点分类。

AI 只负责：

- 根据真实误选和历史表现解释“为什么你会错”。
- 把固定证据组织成可执行的三步解题路径。
- 根据重复错误程度安排复习动作。
- 生成不改变题目事实的即时微验证表达。

模型不得修改答案、原文、依据位置、选项事实、弱点统计或来源。

### 2.2 两层验证，不自由生成完整新题

读解和听力对上下文依赖较强。第一版不让模型自由生成整篇文章或新音频，而采用两层验证：

1. **即时微验证**：仍基于当前材料，检查用户能否重新定位证据、识别转折或排除干扰项。
2. **跨题迁移验证**：从本地已校对题库中选择同一弱点类型的另一道题，正确答案继续由题库锁定。

这样既能验证学习效果，又避免 AI 自由出题带来的答案不稳定和听力题无对应音频问题。

### 2.3 不做无证据推断

允许描述可观察行为，例如：

- “你选择了只复述前半段信息的选项。”
- “你在这类最终决定题上最近答错 3 次。”
- “本题播放 2 次后仍误选了转折前的方案。”

禁止描述无法验证的心理状态，例如：

- “你太粗心。”
- “你的注意力不好。”
- “你没有认真听。”
- “你的阅读能力弱。”

## 3. 产品入口与页面位置

### 3.1 读解模块

入口路径：

```text
首页
  -> 读解训练 / 到期读解弱点
  -> 提交题目
  -> 固定解析
  -> AI 读解辅导
```

页面顺序：

1. 回答结果。
2. 原文证据位置。
3. 核心判断与选项拆解。
4. `AI 读解辅导`入口。
5. AI 诊断、三步路径与即时微验证。
6. 下一题或完成本轮。

仅在答错时主动展示“生成智能辅导”按钮。答对时默认不调用模型，可展示轻量入口“我还是不确定”，由用户主动请求。

### 3.2 听力模块

入口路径：

```text
首页
  -> 听力训练 / 到期听力弱点
  -> 播放并提交题目
  -> 原文和固定解析
  -> AI 听力辅导
```

页面顺序：

1. 回答结果。
2. 音频重播与日语原文。
3. 中文翻译、依据句、关键信号和陷阱点。
4. `AI 听力辅导`入口。
5. AI 诊断、听力重建路径与即时微验证。
6. 下一题或完成本轮。

生成 AI 辅导时暂停正在播放的音频。辅导面板不能遮挡播放控制，用户可以在“三步重听”中重复播放原音频。

### 3.3 本轮结果页

结果页不重复渲染所有 AI 长文本，只显示：

- 本轮主要薄弱点。
- 已生成辅导的错题数量。
- 即时微验证通过数量。
- 尚未通过的题目入口。
- 下一道本地迁移题的推荐入口。

避免结果页变成第二个答题页，也避免用户必须在完成整轮后才看到单题反馈。

## 4. 读解辅导设计

### 4.1 读解输入证据

事实证据全部来自本地题库：

- `passageId`、文章标题和段落文本。
- `questionId`、题干和选项。
- 正确答案与用户误选。
- `evidence`：校对后的证据位置或证据句。
- `explanation`：固定核心判断。
- `choiceInsights`：每个选项的固定分析。
- `reviewNote`、`tags` 和来源。

最小化个人上下文：

- 本题累计错误次数。
- 主要读解弱点：证据定位、主旨判断或干扰项排除。
- 最近 14 天同类错误次数。
- 最近最多 3 个同类题目标识。
- 最近一次即时验证和跨题迁移结果。
- 本次真实误选项。

第一版不采集停留时长、滚动位置或视线数据，因此不得声称用户“读得太快”或“漏看某一段”。

### 4.2 读解输出模块

`AI 读解辅导`包含六部分：

1. **本次误判类型**：说明误选属于证据错位、范围偷换、只说到一半或主旨层级错误。
2. **误选为什么像正确答案**：引用真实误选与固定选项分析。
3. **证据链重建**：按“题干要求 → 原文证据 → 选项改写”展示，不直接复述整篇文章。
4. **三步判断路径**：下次可复用的三步动作。
5. **干扰项对照**：比较正确项和本次误选的决定性差异。
6. **即时微验证**：要求用户完成一次证据定位、改写匹配或干扰项排除。

读解三步路径模板：

```text
1. 先把题干改写成“我要找什么”。
2. 回到指定证据，圈出主语、范围词、转折或结论。
3. 比较选项是否完整保留证据的对象、范围和因果关系。
```

模型必须把模板替换为本题真实线索，不能只返回通用学习建议。

### 4.3 读解即时微验证类型

由服务端根据弱点类型选择一种：

| 弱点类型 | 微验证 | 锁定事实 |
| --- | --- | --- |
| `reading_evidence` | 从 2-4 个原文片段中选真正证据 | 证据段落和证据句 |
| `reading_main_idea` | 在 2-4 个摘要中选覆盖全文判断的一项 | 固定主旨与答案 |
| `reading_distractor` | 指出误选偷换了对象、范围、时间还是因果 | 固定选项分析 |

微验证的正确结果由规则或题库字段确定。模型可以改写展示文本，但不能决定答案。

## 5. 听力辅导设计

### 5.1 听力输入证据

事实证据全部来自本地题库：

- `caseId`、场景、任务和音频标识。
- 日语对话原文及角色。
- `questionId`、题干和选项。
- 正确答案与用户误选。
- `basisLine`：直接支撑答案的台词。
- `keySignal`：转折、否定、追加、变更或结论信号。
- `trapPoint`：本题固定陷阱。
- `choiceInsights`、`reviewNote`、`tags` 和来源。

最小化个人上下文：

- 本题累计错误次数。
- 主要听力弱点：转折漏听、信息追踪、最终结论或主旨判断。
- 最近 14 天同类错误次数。
- 最近最多 3 个同类题目标识。
- 本次播放次数和作答时播放速度。
- 最近一次即时验证和跨题迁移结果。
- 本次真实误选项。

播放次数只用于描述客观行为。例如可以说“本题播放 2 次后仍选择了变更前方案”，不能据此推断注意力或听力能力。

### 5.2 听力输出模块

`AI 听力辅导`包含六部分：

1. **本次漏听类型**：指出错误落在转折、对象对应、信息增减、最终决定或主旨层级。
2. **误选是怎样形成的**：对照真实误选与陷阱点。
3. **听力证据链重建**：按“最初信息 → 关键信号 → 最终结论”重建。
4. **三步重听路径**：告诉用户重听时每一遍具体抓什么。
5. **信号对照**：比较正确结论和误选所依赖的台词。
6. **即时微验证**：验证用户能否识别信号或更新最终结论。

听力三步路径按错误类型适配：

```text
转折/最终决定：先记初始方案 -> 捕捉变更信号 -> 只保留最终决定
信息追踪：列对象 -> 标新增/删除/保留 -> 按题干要求输出对应项
主旨判断：确认场景任务 -> 区分例子与结论 -> 用最后判断复述主旨
即时应答：确认说话意图 -> 判断关系与语气 -> 排除语用不自然回应
```

### 5.3 听力即时微验证类型

| 弱点类型 | 微验证 | 锁定事实 |
| --- | --- | --- |
| `listening_turning_point` | 从台词中选改变结论的信号 | `keySignal`、`basisLine` |
| `listening_detail_tracking` | 更新对象/时间/物品的最终状态 | 对话原文与正确项 |
| `listening_final_decision` | 在初始方案和最终方案中选最终决定 | `basisLine`、`trapPoint` |
| `listening_main_point` | 从细节与结论中选主要意图 | 固定解释与正确项 |

即时微验证复用原音频或校对后的短台词，不在第一版动态合成新音频。

## 6. 跨题迁移验证

### 6.1 选择规则

迁移题从本地题库确定性选择：

1. 模式相同：读解只选读解，听力只选听力。
2. 弱点类型相同。
3. 排除当前 `questionId`。
4. 优先未做过或距离上次作答最久的题。
5. 排除刚刚连续展示过的题。
6. 若没有同类候选，退回相邻弱点类型并明确标记为“综合验证”。

### 6.2 迁移结果

跨题迁移使用目标题自身的固定答案。记录：

```ts
type ComprehensionTransferResult = {
  sourceQuestionId: string;
  targetQuestionId: string;
  modeId: 'reading_drill' | 'listening_analyze';
  weaknessType: ReadingWeaknessErrorType | ListeningWeaknessErrorType;
  selectedChoice: number;
  correct: boolean;
  contextVersion: string;
  answeredAt: string;
};
```

迁移答对不直接清除原弱点，只作为调度晋级的一个信号。原有间隔复习规则仍负责最终掌握状态。

## 7. 数据模型

### 7.1 作答事件

现有 `WeaknessSignalItem` 没有保存用户误选，无法在下次回收时可靠恢复个性化上下文。新增受限长度的作答事件：

```ts
type ComprehensionAttempt = {
  id: string;
  questionId: string;
  modeId: 'reading_drill' | 'listening_analyze';
  selectedChoice: number;
  correctChoice: number;
  correct: boolean;
  errorTypes: WeaknessErrorType[];
  occurredAt: string;
  sessionKind: 'drill' | 'review' | 'transfer';
  listenCount?: number;
  playbackRate?: number;
};
```

约束：

- 仅保留最近 500 条读解/听力作答事件。
- 不保存完整文章、音频或模型提示词副本。
- `correctChoice` 来自本地题库，不接受模型写入。
- 老用户没有事件历史时，从 `WeaknessSignalItem` 安全降级，只显示累计次数，不伪造误选。

### 7.2 专用辅导响应

不强行复用文法/词汇的 `confusionComparison` 语义，新增可区分模式的结构：

```ts
type ComprehensionTutorExplanation = {
  mode: 'reading' | 'listening';
  diagnosisSummary: string;
  whySelected: string;
  reasoningSteps: [string, string, string];
  evidenceChain: Array<{
    label: string;
    text: string;
    sourceRef: string;
  }>;
  decisiveContrast: {
    correctBasis: string;
    selectedBasis: string;
    difference: string;
  };
  reviewPlan: Array<{
    timing: 'now' | 'tomorrow' | 'three_days';
    action: string;
  }>;
  personalizationEvidence: {
    selectedChoice: string;
    wrongCount: number;
    weaknessType: string;
    recentSimilarWrongCount: number;
    listenCount?: number;
  };
  microCheck: {
    kind: 'evidence_location' | 'paraphrase' | 'distractor_rejection'
      | 'signal_detection' | 'state_update' | 'final_decision';
    prompt: string;
    choices: string[];
    answer: number;
    explanation: string;
    lockedEvidenceId: string;
  };
  nextTransferQuestionId: string | null;
  generationMode: 'ai_tutor';
  schemaVersion: 1;
};
```

`answer`、`lockedEvidenceId` 和 `nextTransferQuestionId` 由服务端规则写入，不能直接采用模型输出。

### 7.3 缓存键

```text
comprehensionTutorCache[
  modeId
  + questionId
  + selectedChoice
  + wrongCount
  + weaknessType
  + latestVerificationResult
  + questionContentHash
  + schemaVersion
]
```

以下变化必须令旧缓存失效：

- 误选项变化。
- 累计错误次数变化。
- 主要弱点变化。
- 即时验证或迁移结果变化。
- 题目证据内容变化。
- 响应 Schema 版本变化。

## 8. 服务接口

文法/词汇继续使用现有 `/tutor/wrong-answer`。读解和听力使用独立接口，避免一个请求结构承载三种不同证据语义。

```text
POST /tutor/reading-question
POST /tutor/listening-question
```

读解请求只提交索引和最小学习上下文：

```json
{
  "questionId": "reading-005-q2",
  "selectedChoice": 1,
  "wrongCount": 3,
  "weaknessType": "reading_distractor",
  "recentSimilarWrongCount": 2,
  "recentSimilarQuestionIds": ["reading-002-q1"]
}
```

听力请求额外提交本次客观播放行为：

```json
{
  "questionId": "airport-announcement-001-q1",
  "selectedChoice": 0,
  "wrongCount": 2,
  "weaknessType": "listening_final_decision",
  "recentSimilarWrongCount": 1,
  "recentSimilarQuestionIds": ["clinic-reservation-001-q1"],
  "listenCount": 2,
  "playbackRate": 1.0
}
```

客户端不提交正确答案、证据句或原文作为可信事实。服务端根据 `questionId` 从本地索引读取并锁定这些字段。

## 9. 服务端生成流程

```text
校验请求
  -> 按 questionId 读取本地题库
  -> 锁定正确答案、证据和来源
  -> 计算弱点标签与迁移候选
  -> 构造最小提示词
  -> 模型只生成诊断和教学表达
  -> Pydantic 结构校验
  -> 规则层覆盖所有锁定字段
  -> 内容一致性检查
  -> 返回或安全失败
```

必须校验：

- `questionId` 存在且模式正确。
- `selectedChoice` 是合法索引且与正确答案不同；“我还是不确定”入口允许正确答案，但请求需标记 `uncertainAfterCorrect: true`。
- 三步路径必须正好 3 条。
- `evidenceChain.sourceRef` 全部能映射到本地证据。
- 输出必须明确引用真实误选。
- 微验证答案索引合法且与锁定规则一致。
- 迁移题属于相同模式，并优先匹配相同弱点。
- 输出不得包含“粗心、能力差、没认真”等无证据标签。

提示词把题目内容标记为“只读证据数据”，不得执行文章或对话中可能出现的指令文本。

## 10. 客户端状态与交互

每道题的 AI 面板使用以下状态机：

```text
idle -> loading -> ready
  |        |         |
  |        v         v
  +------ error <- refreshing
```

- `idle`：只显示入口和用途说明。
- `loading`：按钮禁用，允许用户继续查看固定解析。
- `ready`：展示辅导和微验证。
- `error`：保留固定解析，显示“稍后重试”，不阻止进入下一题。
- `refreshing`：保留旧内容，避免界面突然清空。

交互要求：

- 页面离开时取消未完成请求。
- 同一道题只允许一个生成请求。
- 55 秒总超时沿用现有客户端策略，但 12 秒后显示“仍在生成，可先看固定解析”。
- 不自动调用模型，避免每次答错都产生延迟和成本。
- 点击下一题不等待 AI 请求；若请求仍在进行则取消。
- 历史缓存必须明确标记，缓存内容不覆盖新的误选上下文。

## 11. 降级与离线策略

AI 不可用时，训练主流程必须完整可用。

| 情况 | 用户看到的内容 | 行为 |
| --- | --- | --- |
| 无网络 | 固定证据解析 | AI 面板显示离线提示，可继续下一题 |
| FastAPI 不可用 | 尝试 HTTPS 模型代理 | 成功则正常展示 |
| 模型代理不可用 | 固定证据解析 | 不展示半成品 |
| 模型 JSON 无效 | 固定证据解析 | 记录结构失败，允许重试 |
| 本地证据缺失 | 明确提示证据不足 | 不调用模型、不生成诊断 |
| 有匹配缓存 | 历史缓存 | 标记缓存，并按上下文版本校验 |

iOS/Android 真机必须跳过 `localhost`、`127.0.0.1` 和 `::1` AI 服务地址，使用 HTTPS 代理；该规则由移动配置验收持续检查。

## 12. 隐私、成本与安全

- 不上传姓名、账号、设备标识或完整学习档案。
- 只发送当前题目标识、误选索引和聚合后的最小学习统计。
- 正确答案、原文证据和来源由服务端本地数据读取。
- 日志不记录模型密钥、完整提示词或用户全部历史。
- 客户端包不放置真实模型密钥。
- 每个设备限制并发请求；服务端按 IP/匿名安装标识进行合理限流。
- 缓存命中时不重复调用模型。
- 监控每种模式的请求量、成功率、P50/P95 延迟、token 和估算成本。

## 13. 实施顺序

### RLT-0：公共基础

- 修复当前听力辅导开发代码的类型错误。
- 将听力专用组件改为可配置的 `ComprehensionTutorPanel`，但读解和听力文案、证据链分别渲染。
- 新增 `ComprehensionAttempt`、缓存迁移和容量限制。
- 新增读解/听力专用请求与响应 Schema。
- 扩展服务端题目索引，使读解、听力都能按 `questionId` 锁定完整证据。

完成标准：类型检查、Node 测试和 Python 测试通过，老进度数据可无损迁移。

### RLT-1：读解闭环

- 在读解提交后的固定解析下接入 AI 面板。
- 实现三类读解微验证。
- 实现读解迁移题确定性选择。
- 保存微验证和迁移结果。
- 增加读解组件测试与浏览器完整流程测试。

完成标准：证据定位、主旨判断、干扰项排除各至少 5 个固定案例通过人工验收。

### RLT-2：听力闭环

- 在听力固定解析下接入 AI 面板。
- 传入播放次数和速度，但严格限制诊断措辞。
- 实现四类听力微验证。
- 实现音频重播与三步重听联动。
- 实现听力迁移题确定性选择。
- 增加真机音频与 AI 并行状态测试。

完成标准：转折、信息追踪、最终决定、主旨判断各至少 5 个固定案例通过人工验收。

### RLT-3：质量与发布验收

- 扩展真实模型固定评估集：读解至少 24 例、听力至少 24 例。
- 验证同题不同误选、首次错误和重复错误的输出差异。
- 验证事实锁定、结构校验、缓存失效和失败回退。
- 完成 iOS Release 与 Android Preview 真机清单。
- 进行至少 14 天真实使用，比较辅导前后的跨题迁移正确率。

完成标准：达到第 14 节全部发布门槛。

## 14. 验收标准

### 14.1 功能验收

- 读解与听力答错后都能在当前题解析下进入个性化辅导。
- 到期弱点再次进入相应材料时，能使用最新误选上下文生成或读取正确缓存。
- AI 失败不影响固定解析、音频播放、下一题和进度写入。
- 即时微验证和跨题迁移结果都能持久化。
- 关闭 App 后重新打开，辅导缓存和验证结果仍存在。

### 14.2 事实可靠性

- 正确答案被模型改写率：`0%`。
- 证据句、依据台词和来源被模型改写率：`0%`。
- 无本地证据时拒绝生成率：`100%`。
- 微验证锁定答案正确率：`100%`。
- 跨题迁移题使用本地校对答案比例：`100%`。

### 14.3 个性化质量

- 输出引用真实误选项：`100%`。
- 重复错误时使用真实错误次数：`100%`。
- 三步路径包含当前题真实证据或信号：目标 `>= 95%`。
- 禁用无证据负面标签：违规率 `0%`。
- 人工评分中“比固定解析提供了新增、可执行价值”：目标 `>= 90%`。

### 14.4 工程与真机验收

- `npm test`、`npm run typecheck`、`npm run test:ai` 全部通过。
- 读解与听力 Web 主流程测试通过。
- 无 `.env.local` 时 iOS Release bundle 仍包含 HTTPS AI 代理。
- iPhone 真机在 Wi-Fi、蜂窝网络、弱网和断网场景行为符合降级设计。
- 听力播放、暂停、重播、切题和 AI 请求之间不存在音频串题或状态卡死。
- P95 响应时间、失败率和单次估算成本进入发布报告。

## 15. 明确不在第一版实现的能力

- 开放式连续聊天。
- AI 自由生成完整读解文章。
- AI 动态生成无校对音频。
- 根据停留时间推断阅读能力。
- 根据播放次数推断注意力或学习态度。
- 自动清除弱点或替代现有间隔复习规则。
- 开放式全网检索后直接改变题目事实。

## 16. 最终完成定义

读解或听力模块只有同时满足以下条件，才算“AI 个性化辅导完成”：

1. 当前题页面有明确入口且真机可用。
2. AI 使用真实误选和最小学习历史，而不是只润色固定解析。
3. 所有答案、证据和来源均由本地题库锁定。
4. 有即时微验证，也有本地校对题跨题迁移。
5. 验证结果写回缓存版本和复习调度。
6. 网络失败不破坏本地训练主流程。
7. 自动测试、真实模型评估和 iOS/Android 真机验收全部通过。

达到以上定义后，读解和听力才与现有文法、词汇个性化辅导共同构成完整的四模块 AI 学习闭环。
