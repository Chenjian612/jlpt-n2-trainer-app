# JLPT N2 Trainer App — 工程基线收口

日期：2026-09-11。基于当前 `main` 的开发基线。

## 当前阶段

M0 可用、M1 有效已完成；M2 可信仍需真实学习效果和发布前验收。AI-M0～AI-M4 已有实现，但工程测试通过不等于已验证长期学习收益。

文法/词汇错题的基础 Leitner 已在历史提交 `fa6b57e` 实现：答对晋级、答错退回第 1 箱、到期筛选、第 5 箱掌握。此前路线图的“尚未启动”有误，现已纠正。读解/听力弱点与学习包也已纳入统一间隔调度，但 M3 仍需继续完成真实使用验证和更多模式覆盖。

## 本次变更

- AI 缓存测试统一使用固定测试时钟，不再受实际运行日期影响。独立检查有效期边界、过期和内容哈希。
- 新增 `.github/workflows/ci.yml`：push、pull request、手动触发三个独立任务，检查 Node/TypeScript、Python AI、Chromium。
- 浏览器测试针对禁用本地环境文件的 Web 生产导出，自动使用空闲端口启动服务器，运行后关闭。失败上传截图、日志与 Playwright trace，保留 7 天。
- 更新词卡数量和 Leitner 最终晋级的测试数据；听力按真实题目列表逐题推进，取消固定等待及未找到即时应答仍通过的路径。
- 修复听力即时应答按钮启用但提交被播放条件拦截的问题，统一按钮与提交判断。
- 补齐 `official-m4q1-q1` 第 4 个选项解析，修复提交后白屏；新增全部听力选项解析完整性检查。内容数量未变化。

## 验证范围

本地验证命令：`npm test`、`npm run typecheck`、`npm run test:ai`、`npm run build:web:test`、`npm run test:web`。

本地结果：Node 63/63、Python AI 41/41、Chromium 10/10 全部通过；TypeScript 检查、Web 生产导出、工作流 YAML 解析和 `git diff --check` 通过。浏览器在 macOS Chromium 执行，CI 配置使用 Ubuntu、Node 22 和 Python 3.12；远端 CI 已完成首次验证。

## M3 第一阶段进展

- 新增统一复习调度服务，统一计算文法错题、词汇错题、文法学习包、词汇学习包、官方词卡、读解弱项和听力弱项七类待复习任务。
- 首页计划会显示待复习总数，并按最早到期任务排序；已完成某模式但仍有待复习项时，该模式会继续留在计划中。
- 读解会优先打开含有待复习弱项的文章；听力会优先安排含有待复习弱项的案例。
- 学习包继续使用 4 小时冷却；错题继续使用基础 Leitner。所有复习判断改用本地日历日期，覆盖上海和洛杉矶时区的午夜边界。
- 增加了状态、日期边界、首页计划、读解定位、听力定位和结果回写测试。
- 弱项和学习包现在也按 1/2/4/8 天间隔逐级晋级，第 5 级才移出复习队列；答错会退回第 1 级。
- 首页新增“学习效果信号”：待复习数量、重复错误数量、已晋级复习项、迁移题正确率、错误趋势和近 7 天复习轮数，避免只用训练轮数判断效果。
- 官方词卡的“模糊”和“没记住”现在会持久化；到期后进入首页计划，相关词包置顶并直接打开待复习词卡，答对后继续按 1/2/4/8 天晋级。

CI 已完成远端验证。未将任何检查设置为 GitHub 分支保护必需项。

浏览器覆盖错题回收、词卡翻卡、首页联动、听力播放门槛、即时应答及完整 26 题结果记录；尚未覆盖所有模式、移动端、真实模型和生产部署。听力使用本地 MP3 验证播放链路，不代表其他案例已经具备匹配的授权音频。

## 下一步

1. 连续真实使用，记录推荐合理性、迁移正确率和重复错误趋势，完成 M2 效果验证。
2. 评估并接入文法 200 条的不稳项与排序错题调度。
3. 补充匹配案例的授权听力音频，并完成逐条验收。
4. 完成 Web 生产验收，再准备移动端分发。
